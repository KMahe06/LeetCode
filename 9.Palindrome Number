class Solution {
    public boolean isPalindrome(int x) {
           int rn=0,y=0; 
           int tp=x;
            while(tp>0){

                y=tp%10;
                rn=rn*10+y;
                tp=tp/10;

            }
            if(rn==x) return true;
            else return false;
    }
}
